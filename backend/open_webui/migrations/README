Generic single-database configuration.

Create new migrations with
PYTHONPATH=../backend DATABASE_URL=sqlite:///data/webui.db alembic revision --autogenerate -m "a description"

Update custom_migrations when new migrations are added.
